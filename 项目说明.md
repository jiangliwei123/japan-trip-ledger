# 日本旅行记账 · 项目说明（跨对话交接文档）

> 用途：本项目任何**新对话**的 agent 都应先读此文件，即可无缝接上已有的账目与规则。
> 如本文件不在上下文里，请让用户在对话中输入：`读 /workspace/项目资产/项目说明.md 并继续日本旅行记账`
> （也可在资产库中 @ 引用本文件；本文件可上传到资产库后再引用。）

---

## 1. 项目目标

为「姜」和「张」两人的日本旅行做记账助手，交付两份产物：
- **交互网页** `index.html`：手机端友好、可切换日元/人民币、成员对比、AA 结算、成员筛选。
- **Excel** `trip_account.xlsx`：含「消费明细 / 汇总 / AA结算」三表。

每次有成员报新消费，更新数据后**重新生成这两份文件**即可。

---

## 2. 成员

| 代号 | 真实名 | 角色 |
|------|--------|------|
| A | 姜 | 项目所有者（你），第二个成员名已确认为「张」，**不是**「成员B」 |
| B | 张 | 同伴（另一成员），项目 id 中对应成员 id `6cb48c13-4678-48d4-94e0-903b0f11b511` |

> ⚠️ 网页与 Excel 里第二位成员一律显示「张」，不要再用「成员B」占位。

---

## 3. 文件位置（全部在 `/workspace/项目资产/`）

| 文件 | 作用 |
|------|------|
| `data.json` | **唯一数据源**：成员、汇率、所有消费明细 |
| `gen.py` | 生成脚本：读 `data.json`，输出 `index.html` + `trip_account.xlsx` |
| `index.html` | 交互网页（数据已内嵌） |
| `trip_account.xlsx` | Excel 账目 |
| `筛选功能_证据截图.png` | 给用户的验证截图（可删） |

---

## 4. 数据模型（`data.json`）

```json
{
  "meta": {
    "trip": "日本旅行",
    "members": ["姜", "张"],
    "currency_rate": 21.0,
    "rate_note": "1 元 ≈ 21 日元（近似汇率，可在网页中实时调整）",
    "updated_at": "2026-07-31 16:00"
  },
  "transactions": [
    {
      "id": 1,
      "date": "2026-07-31",
      "payer": "姜",
      "item": "日本签证",
      "amount_cny": 859.32,
      "shared": false,
      "category": "签证"
    }
  ]
}
```

### 记账规则（核心）

- **实际支出**：付款人记全额。如张花 600 订酒店 → 张实际支出 +600。
- **消费（AA 后）**：
  - `shared: true`（AA 平分）→ 双方各 +金额/2。
  - `shared: false`（单人）→ 仅付款人 +全额，对方消费不变。
- **AA 结算**：算出「谁该给谁多少钱」。张净额为正→张需补给姜；为负则相反；≈0 则已平账。

### 新增一笔消费（标准流程）

1. 编辑 `data.json`，在 `transactions` 追加一条（自增 `id`）。
2. 运行：`python3 /workspace/项目资产/gen.py`
3. 产物自动覆盖 `index.html` / `trip_account.xlsx`。

> ⚠️ 始终改 `data.json` 后重生成，**不要手写** HTML/Excel。

---

## 5. 当前账目状态（截至 2026-07-31）

| 付款人 | 项目 | 金额(元) | 分摊 |
|--------|------|---------|------|
| 姜 | 日本签证 | 859.32 | 单人 |
| 姜 | 天津→东京成田 机票 | 1134.00 | 单人 |
| 姜 | 日本硬币夹 | 2.30 | 单人 |

- 总计 **¥1995.62**；张暂未报任何消费。
- 当前净额均为 0，无需结算。

---

## 6. 网页功能要点

- 顶部 **元 / 日元** 切换（汇率 `currency_rate`，可在 `data.json` 调整）。
- **成员对比卡**：分别显示「实际支出」与「消费」。
- **支出构成** 圆环（按类别占比）。
- **消费明细**：支持 **全部 / 姜 / 张** 成员筛选（按付款人过滤，筛选时顶部显示该成员笔数+小计）。
- **AA 结算卡**：自动算「X 需向 Y 支付 ¥Z」。

---

## 7. 预览链接（实时访问最新版）

已用 `preview` 技能起静态服务器，从磁盘实时读取，地址稳定、永远是最新版：

- 网页：`https://webview.e2b.sh1.sandbox.cloudstudio.club/?x-cs-sandbox-id=e9b093888fb04d42b4bf96cd88ca54cf&x-cs-sandbox-port=8000`
- Excel（同地址加路径）：`…8000/trip_account.xlsx`

> 服务器由 supervisord 托管（端口 8000，目录 `/workspace/项目资产/`，命令 `python3 -m http.server 8000 --bind 0.0.0.0 --directory /workspace/项目资产`）。沙箱休眠后会自动重启；若沙箱彻底重建则链接失效，需重新 `notify 8000` 出新地址。

---

## 8. 重要限制（曾踩过的坑）

- **不能直接写项目资产库**：`mcp__netdrive` 对个人版项目资产库不可见（实测搜「项目」0 命中），无暴露的「上传到资产库」API。指南里的「上传到云端」是**对话 UI 按钮**（用户操作），agent 无法代点。
  - ✅ 正确做法：对话里对生成的产物点「上传到云端」→ 选 costinJapan 资产库（可覆盖旧版）。
- **ShareLink 工具被环境拦截**（403，场景限制），无法生成固定公开链接；改用 `preview` 技能的 `notify` 出预览地址。
- **agent 只读资产库、不可写**：可用 @ 引用或 `tdrive_file_id` 读取资产库文件，但不能原地修改/覆盖。

---

## 9. Git 仓库（跨沙箱唯一真相源）

仓库（私有）：`https://github.com/jiangliwei123/japan-trip-ledger.git`
本地仓库已在 `/workspace/项目资产/`（已 `git init` 并提交）。

**本环境推送到远程（已有 gh 登录与令牌）：**
```
cd /workspace/项目资产
python3 gen.py            # 改完 data.json 后先重生成
git add -A && git commit -m "记账更新：<说明>" && git push
```

**新沙箱（全新环境、无本地文件）开局：**
```
git clone https://github.com/jiangliwei123/japan-trip-ledger.git /workspace/项目资产
cd /workspace/项目资产 && python3 gen.py
```
> 私有库 clone 需凭据：让用户在对话提供 GitHub PAT（有 repo 权限），或本环境 gh 已登录则直接 clone。

**固定公网链接（可选）：** 仓库开启 **GitHub Pages**（Settings → Pages → 选 main 分支根目录），即可获得稳定网址直接访问 `index.html`，push 即更新。

---

## 10. 新对话开局话术（给用户）

> 两种情况：
> 1. 若本环境已有 `/workspace/项目资产/` 文件：直接说「继续日本旅行记账，读 项目说明.md，然后我报新消费」。
> 2. 若是全新环境：让用户先 `git clone` 仓库（或提供 PAT 让我 clone），再同上。

或直接 @ 引用本文件后说「继续记账」。

---

## 11. 待办 / 可选项

- [ ] 张报消费后，按第 4 节流程入账并更新本文件第 5 节状态。
- [ ] 如有准确汇率，更新 `data.json` 的 `currency_rate`。
- [ ] （可选）消费明细增加「日期范围筛选」。
- [ ] （可选）仓库开启 GitHub Pages，得到固定公网链接。
- [ ] （可选）把本文件上传到资产库，方便新对话 @ 引用。
